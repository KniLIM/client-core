import React from 'react';
import {BrowserRouter} from 'react-router-dom';
import Router from 'app/Router';

import './App.css';


export default () => (
    <BrowserRouter>
        <Router />
    </BrowserRouter>
);
